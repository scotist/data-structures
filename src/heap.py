# _*_ encoding: utf-8 _*_


class Heap(object):
    """Make a binary heap."""

    def __init__(self, val=None):
        """Initialize heap."""
        heap = []

    def pop():
        """Pope first item off of heap."""

    def push(val):
        """Push value into heap."""

    def heapify(heap)
